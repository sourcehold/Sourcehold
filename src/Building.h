#pragma once

#include <Config.h>

#include <Rendering/Rendering.h>
#include <Rendering/Tileset.h>

namespace Sourcehold
{
    namespace Game
    {
        class Building
        {
            public:
                Building();
                ~Building();
            private:
        };
    }
}
